import React, { useState } from "react";
import Message from "./Message";

const ChatPanel = ({ messages, sendMessage, messageText, setMessageText, userData }) => {
    return (
      <></>
    );
  };
  
export default ChatPanel;
